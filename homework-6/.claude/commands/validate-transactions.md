Validate all transactions in `sample-transactions.json` without running the full pipeline (dry-run).

Steps:
1. Run: `cd homework-6-kotlin && ./gradlew run --args="--dry-run"`
2. Report: total count, valid count, invalid count
3. Show the table of transaction IDs, statuses, and rejection reasons